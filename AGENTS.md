# ASOBIBAR Y2K NIGHT LP — agent contract

このファイルが正典です。Codex はこれを直接読み、Claude Code は `CLAUDE.md` から
ここへ来ます。**内容を二重に持たないでください。**片方だけ更新されると、
どちらが古いのか誰にも分からなくなります。

## Project

ASOBIBAR（マッチングバー）の 2026年9月イベント「Y2K NIGHT」の
**1ページのランディングページ**です。素の HTML / CSS / JS のみ。
ビルド工程もフレームワークもありません。GitHub Pages で公開しています。

- `index.html` / `styles.css` / `script.js` の3つが本体
- `assets/` に画像・動画・セルフホストしたフォント
- テストもステージングも無い。**壊れても差分では見えず、公開してから気づく**

## 何が必ず走るか（誰が操作していても）

読む人にしか効かない文章と違い、これらは git hook で実行されます。

```bash
node scripts/harness/harness-check.mjs                       # 差分に対する不変条件
node scripts/harness/harness-run.mjs --stage pre-push --dry-run
node scripts/harness/new-plan.mjs task <slug> --title "..."  # exec-plan / adr / gotcha
```

- `harness/contracts/asobibar-lp.yaml` — **このLP固有の不変条件**（実際に事故った箇所だけ）
- `harness/contracts/workflow.yaml` — 計画と検証記録の作法
- `.githooks/` — clone ごとに1回 `sh scripts/install-hooks.sh`

### 共通部分は、このリポジトリの外にあります

ハーネスは**エンジンと汎用ルールを他プロジェクトと共有**しています。実体は
`../` （`Job/LP/`）に1部だけあり、ここからは symlink で参照しています。

| 参照（symlink） | 実体 |
| --- | --- |
| `scripts/harness/` | `../../scripts/harness` |
| `harness/contracts/{workflow,secrets}.yaml` | `../../../harness/contracts/` |
| `harness/scenarios/{workflow,harness-self-test}.yaml` | `../../../harness/scenarios/` |
| `harness/*/\_examples/`、`harness/README.md` | 同上 |

**このLP固有なのは実ファイルのものだけ**です:
`harness/contracts/asobibar-lp.yaml` と `harness/scenarios/asobibar-lp.yaml`。
LPのルールを足すならこの2つに書いてください。**symlink 側を編集すると、
それを参照している他のプロジェクト全部に効きます。**

**`git clone` しただけでは動きません。** symlink はリポジトリの外を指しているので、
共有元が無い環境では8本すべてが dead link になり、`harness-check.mjs` は
モジュールが見つからず起動しません。hook を入れていれば、その時点で
コミットも通らなくなります。

共有元（`lp-harness`）を先に取得し、その場所を指定して実ファイルに戻してください。
プロジェクト内の相対パスは共有元の相対パスとそのまま一致します:

```sh
SHARED=/path/to/lp-harness          # 共有ハーネス（このマシンでは ../）を置いた場所
for l in $(find . -type l -not -path './tools/*'); do
  rm "$l" && cp -R "$SHARED/${l#./}" "$l"
done
```

戻したあとは `node scripts/harness/harness-check.mjs` が動くことを確認します。
**この状態でコミットすると symlink が実ファイルに戻った差分が乗ります。**
共有構成を維持するなら、その差分は捨ててください。

**作業完了を報告する前に `harness-check.mjs` を走らせてください。**読み取り専用です。

`[manual]` と出る行は、機械が判定できない規則です。**あれが確認事項の本体**で、
飾りではありません。毎回 `manual` と出続けます。

## このプロジェクト固有の作法

**本文で使える書体は3つだけ。** `var(--font-main)`（角ゴシック）/ `var(--font-display)`
（丸ゴシック）/ `var(--font-hand)`（手書き）。セルフホストしたサブセットしか手元にありません。
**見出しや本文に新しい漢字を足したら `python3 tools/build-fonts.py` を流す。**
忘れるとその字だけ別書体に化けます。かな・ASCII・約物は保険で全部入っています。

**4つ目だった `var(--font-yankee)`（Reggae One）は 2026-09-04 に落としました。**
MIX の「交流上等」に使っていましたが、**あれは「上等 → 特攻服 → ヤンキー漫画」という
私の推測で選んだ書体**で、依頼者が参考にしているものとは関係がありませんでした。
いまは `var(--font-display)`（Mochiy Pop One）です。`@font-face`・変数・
`tools/build-fonts.py` の行・`assets/font/reggae-400.woff2` すべて削除済み。
**書体を増やす前に、何を参考にしているのか実物を見ること。**

**4つ目の `var(--font-dot)`（DotGothic16）は見出しとラベル専用です。**（2026-09-04 追加）
2000年代の画面の記号として入れました。**本文には広げないでください。**
料金・注意書きは `var(--font-main)` のままです
（`lp-visual-knowledge` §4「読ませたい場所に世界観を持ち込まない」）。

- 収録文字はページ全文字。ドット書体はグリフが単純なので **23.1KB** で入ります
- **この書体だけ OpenType 機能を落としています**（44.3KB → 21.9KB）。
  中身を見て、`kern` も `palt` も `ccmp` も持っていないことを確認済み。
  **他の書体で同じことをしないでください。**理由は `tools/build-fonts.py` に書いてあります
- **`font-weight: 900` を当てないこと。** DotGothic16 は 400 しか無いので、
  ブラウザが合成ボールドを掛けてドットが潰れます
- **`letter-spacing` の詰め（-0.045em〜-0.06em）も一緒に外すこと。**
  ドットは字面が小さいので、詰めるとドット同士がぶつかります。`0.005em` にしてあります
- **見出しに当てるときは詳細度を確認すること。** 最初 `.paper-h, .scene-title, #last-title`
  (0,1,0) だけで書いて、**8つの見出しのうち3つにしか効いていませんでした。**
  セクションごとの作り込みで後から書いた `.scene--karaoke .scene-title` (0,2,0) や
  `.scene--request .request-heading .paper-h` (0,3,0) に負けていた
  （`#last-title` だけ効いていたのは ID だったから＝偶然）。
  **`!important` では直していません。**ファイル末尾で同じセレクタを書き直して勝たせています
- **当たっているのは6つの節見出しだけ。** `.howto-title`（英字ロックアップ）と
  `.mix-lock`（MIXのロゴ）は別の記号なので**あえて当てていません**。
  MIX のキッカーと下段（`.mix-kicker` / `.mix-foot b`）にはドットを当てています

**`assets/` は全除外方式です。** `.gitignore` が `assets/*` を除外し、必要なものだけ
`!assets/...` で戻しています。**新しい画像を参照したら許可リストに足す。**
忘れると公開サイトでだけ404になり、ローカルでは最後まで気づけません。

**`index.html` のクエリ番号を上げる。** `styles.css?v=N` / `script.js?v=N`。
上げないと閲覧者には旧版が出続けます。HTML自体にはバスターを付けられないので、
**HTMLだけの変更は強制再読み込みでしか確認できません。**

**アプリ内のブラウザペインでも表示できます（2026-09-04 に直しました）。**
以前は真っ白になっていました。原因は「アプリ内ブラウザの都合」ではなく**このLPの作り**です:
`motion-ready` / `reveal-ready` を付けた時点で本文を `opacity: 0` にしていたので、
**IntersectionObserver のコールバックが返らない環境ではページから中身が消えていました。**
`"IntersectionObserver" in window` は true になるため、旧来のガードでは検出できません。

`script.js` の `probeObserver()` が、1pxのダミーを1つ観測して
**監視が実際に返ってくることを確かめてから隠します。**400ms 返らなければ
アニメーションのほうを捨てて中身を出します（`prefers-reduced-motion: reduce` と同じ扱い）。
Hero は `rAF` 2回と `setTimeout(300)` を併走させ、先に来たほうで着地します。

**隠す指定を新しく足すときは、必ず `probeObserver()` の内側に置いてください。**
外に書くと、また同じことが起きます。docs/design-docs/lp-visual-knowledge.md §6。

**寸法を測るときは実機Chromeをヘッドレスで。** ペインは遅延画像の読み込み中に
描画が追いつかず、スクロール中のコマが白く写ります。**見るのはペインでよく、
測るのはヘッドレスで**（`scripts/harness/` とは別に、その都度用意する）。
測定スクリプトでは先に `document.documentElement.style.scrollBehavior = "auto"` を
入れること。`scroll-behavior: smooth` と競合して、着地前の値を読みます。

## デザインの方向性（変更には合意が要る）

「平成のシールとテープの誌面」。線・影・傾きを足していいのは**写真・シール・マステだけ**。

**2026-09-04、「2000年代の画面」の記号を3手入れて、そのうち2手は同日に戻しました**
（D案。依頼者が4案から選択 → 実物を見て②③を差し戻し）。参考にしたのは
『ダンダダン』5周年特設サイト。計画と実測は
[docs/exec-plans/active/y2k-visual-pass-d.md](./docs/exec-plans/active/y2k-visual-pass-d.md)。

| 手 | いまの状態 |
| --- | --- |
| ① 見出しとラベルをドット書体 | **入っている。**`var(--font-dot)`。**本文は当てない** |
| ② 写真を2色ハーフトーン（青） | **戻した。** 写真は元の色。`.duo` はもう存在しない |
| ③ CTAをべた塗りマゼンタ | **戻した。** CTAは元のローズ面＋クローム外周 |

**②③はセットです。** ②で写真の彩度が上がるぶんCTAを面に替えたので、
片方だけ戻すと**写真がCTAより強くなります**（`lp-visual-knowledge` §2 / §5）。
足し直すときも外すときも2つ一緒に。

**書体だけなら彩度に触らないので、CTAは元のままで沈みません。** これが今の形です。

### 方向A（「全部ウィンドウの中」）— **保留中。実装は入っていません**

**2026-09-04、依頼者が「A」をLP全体の方向として選びました。** MIXで先行実装しましたが、
**同日に依頼者から「デザインがダメすぎる」で差し戻し**、MIXは別の組み方に置き換えました。
**いまLPのどこにもAは入っていません。**（`.mix-win` / `.mix-seek` / `.mix-panel` は削除済み）

**なぜ外したか。** MIXは番組バナーに寄せる話だったのに、私が
「全部ウィンドウの中に入れる」というAの決定を先に置いて、番組の画をその中に押し込みました。
**ウィンドウ枠・シークバー・`rule.ini` はデスクトップの言語で、バナーの言語ではありません。**
1つの画面に2つの文法が入って、寄せたかった側が負けました。

**Aを他の節で試すときは、その節が別の言語を要求していないか先に確かめること。**
それと、地の `#003ed6` は実測で `彩度×明度 = 0.839`（CTAバーの平均は `0.449`）で、
**べた塗りで広げるとCTAが一番強い色ではなくなります。**候補の実測値は
`#000080` → 0.50、`#001a66` → 0.400、`#a8c8f0` → 0.282。数字は
[docs/exec-plans/active/mix-band-section.md](./docs/exec-plans/active/mix-band-section.md)。

### MIX のキーアート（いまの形）

**借りているのは構図だけです。** 色の違う2つの世界を継ぎ目で突き合わせ、その上に
中央寄せの文字段を1本立てる（小さいキッカー → 大きいロゴ → 罫で挟んだ一行 → 下段の帯）。
番組バナー一般の型で、SPは上下、768以上で左右に割れます。

**写真は加工していません。** 左右の寒暖差は**写真そのものが持っている色**で作っています
（左＝`karaoke-crowd-v2.jpg` の青いネオン、右＝`howto-color-band-scene.jpg` のマゼンタ照明）。
乗せているのは文字を読ませるための暗幕だけ。**`.duo` の色替えは差し戻し済みなので、
ここで写真の色相を動かさないでください。**

**彩度を持つのはマゼンタの文字だけです。** 面には出していないので、走査すると
**`.ink-button`（予約CTA）がページで最も彩度の高い面**として1位に戻ります。

**`howto-color-band-scene.jpg` は 07 HOW TO でもサムネイルとして使っています。**
店から男女それぞれのグループ写真が来たら、右半分はそちらに差し替えるのが本来です。

**MIXは実在の配信作品のパロディという狙いで作っています。** 記号は**ジャンル一般のものだけ**
（白抜き太ゴシックのテロップ、エピソード表記、ナレーション調、金の刺繍風タイトル）。
**実在番組のロゴ・タイトルの字組み・キーアート・配信社のマークは使いません。**
商用ページで実在ブランドを流用すると、パロディの意図でも商標の問題になります。
**この線引きは私の判断で、法務確認は取っていません。**

**`.duo` を書き直すことがあれば、2回踏んだ穴を先に見てください**
（コードは消えましたが、実測は exec-plan に残してあります）。
- `position: relative` を `.request-photo__screen` に足すと、中の絶対配置の画像が
  **366×275 → 366×0** に潰れます。位置基準は元から持っている枠にだけ
- 網点は **`::before`** で。`::after` は `.karaoke-photo` と `.final-night__photo` が
  **白文字を読ませるための暗いスクリム**に使っています。上書きすると文字が沈みます

**却下済み（再提案しない）**: ニューブルータリズム、真っ黒の地、CSSで描いた抽象背景、
巨大タイポの復活、意味の通らない英字、大量のシール、**紫**。

**触らない**: 背景、写真、固定予約CTA。

**FV・主要コピーは 2026-09-04 に依頼を受けて組み直しました。**
以前ここに「触らない」と書いてあったものです。理由は
[docs/exec-plans/active/lp-restructure-plan-a.md](./docs/exec-plans/active/lp-restructure-plan-a.md)。

**セクション順は同日に動かして、同日に元へ戻しました。**
いまの並びは**元の並びと同じ**（削除した `#song-pet` を除く）。
**これが正典です。**

| P | id | 役 | 中身 |
| --- | --- | --- | --- |
| 01 | `#top` | 掴み | 表紙。キービジュアル＋CTAだけ |
| 01 | `#experience` | ベネフィット | 曲リクエスト |
| 02 | `#karaoke` | ベネフィット | カラオケ |
| 03 | `#cheki` | ベネフィット | チェキ |
| 04 | `#mix` | ベネフィット | ミックス交流上等バンド（キーアート） |
| 05 | `#price` | 条件 | 料金 |
| 06 | `#event` | 条件 | 開催情報 |
| 07 | `#howto` | 条件 | HOW TO（圧縮版） |
| 08 | `#reserve` | CTA | 特典＋最終CTA |

**この「ベネフィット → 条件 → CTA」の順を崩さないでください。**
`lp-cvr-knowledge` の「悩み → 共感 → ベネフィット → 証拠 → CTA」です。
**2026-09-04 に料金と開催情報を体験より前へ出して、依頼者から差し戻されました**
（[docs/gotchas/section-order-benefit-before-price.md](./docs/gotchas/section-order-benefit-before-price.md)）。
**料金セクションを前に出して順序を直そうとしないでください。**同じ問いを2回解くと順序が壊れます。
なお **FVに置いていた `期間 / 料金 / 特典` の3行は、2026-09-04 に依頼者の判断で削除しました。**
その結果、**「いくら」に1画面目で答える手段はいま1つもありません**（既知・下記 risk）。

**`data-page` は表紙が次のセクションと番号を共有します**（`#top` と `#experience` が両方 01）。
節を足し引きしたら、`data-page`・`.scene-label` の `<b>`・`.bar__nombre` の
`/P.08` の3つを揃えること。ノンブルは `data-page` をそのまま出すだけです。

**動かす前に読むこと:**

- **無料フードチケットは最終CTAの直前（`.final-perk`）に1つだけある。**
  元は HOW TO の3枚目の末尾（**86.1%地点**）。一度FVにも出したが、
  **FV側は 2026-09-04 に依頼者の判断で削除した。**押す理由でいちばん強い一言なので、
  **残っている1つは消さない。**
- **FVはキービジュアルとCTAだけ。** `.cover__info` は ≥900px で絶対配置なので、
  ここに要素を足すとCTAが1画面目から出る。`top: min(72%, calc(100svh - 150px))` の
  150px は **66px（スティッキーバー）＋ 70px（CTA）＋ 逃げ**で決めてある。
  **足したら 8サイズ（1440×900 / 1920×1080 / 2560×1440 / 1280×720 / 1024×768 /
  900×700 / 390×844 / 375×667）でCTAの下端を測ること。**130px で
  1280×720 が 6px はみ出した実績がある。
- **順番を動かすときは、変更前と変更後の両方を
  「悩み → 共感 → ベネフィット → 証拠 → CTA」にマッピングして exec-plan に書くこと。**
  読んだだけでは効きませんでした。書き出すと飛ばしたことが目に見えます。
  **機械の検証が全部通っても、順番が良いことの証明にはなりません。**
  harness が見ているのはノンブルの連番・CTAの死角・コントラストだけです。
- **HOW TO は 2,420px（総丈の27.6%）から 788px に畳んである。** 3ステップ版
  （`.howto-step` / `.howto-index`）は本文から外したがCSSは残っている。
  流入の半分は店を知らない人なので**セクションごと消さない。**
- **「この夜に起きること」（`#tonight`）と FAQ（`#faq`）は 2026-09-04 に足して
  同日に消しました。** 依頼者の判断です。**再提案しないでください。**
  要約3枚は後段のセクションの言い換えで、FAQ は答えを持っている4問しか置けなかった
  （入店時刻・年齢確認・キャンセル規定は店舗の回答待ち）。
  **読ませるだけの節を足すなら、`.scene` ではなく `.sheet`（紙）に載せること。**
  `.scene` は `background: var(--night-violet)` の暗い地なので、
  `.paper-h`（`--print-ink`）を載せると見出しが読めません。1回やりました。


## Non-negotiables

- **`scripts/harness/` `harness/scenarios/` `.githooks/` の編集は、hookが入っている
  全端末での任意コード実行と同じです。** シナリオの `run:` は誰にも見せずに実行される
  シェルです。設定ではなく hook としてレビューしてください。
- 秘密情報はリポジトリにもクライアント側コードにも入れません。ブラウザに載ったものは
  公開された時点で公開情報です。
- **`TODO(店舗確認)` を推測で埋めない。** 年齢制限・身分証確認・キャンセル規定は、
  間違えると客とのトラブルになります。店舗の回答が来るまで空欄のままにします。
- **`main` への push は本番公開です。** 自動で走らせないでください。

## Tool-specific setup

共有: `harness/` `scripts/` `.githooks/` `docs/`。
形式が違うため意図的に統一していないもの:

- Claude Code: `.claude/settings.json`（権限・hook）、`.claude/agents/`
- Codex: `.codex/config.toml`、`.codex/agents/*.toml`
